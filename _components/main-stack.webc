<div><slot></slot></div>

<style webc:scoped>
:host div {
  display: flex;
  flex-direction: column;
  gap: var(--sl-spacing-large);
}

@media (min-width: 800px) {
  :host div {
    gap: var(--sl-spacing-2x-large);
  }
}
</style>
